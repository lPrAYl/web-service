{
  "name": "web-service",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "b": "pnpm --filter @web-service/backend",
    "w": "pnpm --filter @web-service/webapp",
    "dev": "pnpm -r --parallel dev",
    "types": "pnpm -r --parallel types",
    "prettify": "pnpm -r --parallel prettify"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "prettier": "^3.6.2",
    "rimraf": "^6.0.1",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.3"
  }
}